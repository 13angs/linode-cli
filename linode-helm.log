usage: linode-cli [--help] [--text] [--delimiter DELIMITER] [--json]
                  [--markdown] [--ascii-table] [--pretty] [--no-headers]
                  [--all] [--all-columns] [--format FORMAT] [--no-defaults]
                  [--no-truncation] [--no-retry] [--column-width COLUMN_WIDTH]
                  [--version] [--debug] [--page PAGE] [--page-size PAGESIZE]
                  [--all-rows] [--as-user USERNAME] [--suppress-warnings]
                  [COMMAND] [ACTION]

The Linode Command Line Interface. Aliases: lin, linode

positional arguments:
  COMMAND               The command to invoke in the CLI.
  ACTION                The action to perform in this command.

options:
  --help                Display information about a command, action, or the
                        CLI overall.
  --text                Display text output with a delimiter (defaults to
                        tabs).
  --delimiter DELIMITER
                        The delimiter when displaying raw output.
  --json                Display output as JSON.
  --markdown            Display output in Markdown format.
  --ascii-table         Display output in an ASCII table.
  --pretty              If set, pretty-print JSON output.
  --no-headers          If set, does not display headers in output.
  --all                 Deprecated flag. An alias of '--all-columns',
                        scheduled to be removed in a future version.
  --all-columns         If set, displays all possible columns instead of the
                        default columns. This may not work well on some
                        terminals.
  --format FORMAT       The columns to display in output. Provide a comma-
                        separated list of column names.
  --no-defaults         Suppress default values for arguments. Default values
                        are configured on initial setup or with linode-cli
                        configure
  --no-truncation       Prevent the truncation of long values in command
                        outputs.
  --no-retry            Skip retrying on common errors like timeouts.
  --column-width COLUMN_WIDTH
                        Sets the maximum width of each column in outputted
                        tables. By default, columns are dynamically sized to
                        fit the terminal.
  --version, -v         Prints version information and exits.
  --debug               Enable verbose HTTP debug output.
  --page PAGE           For listing actions, specifies the page to request
  --page-size PAGESIZE  For listing actions, specifies the number of items per
                        page, accepts any value between 25 and 500
  --all-rows            Output all possible rows in the results with
                        pagination
  --as-user USERNAME    The username to execute this command as. This user
                        must be configured.
  --suppress-warnings   Suppress warnings that are intended for human users.
                        This is useful for scripting the CLI's behavior.

Environment variables:
┌────────────────────────┬─────────────────────────────────────────────────────┐
│ Name                   │ Description                                         │
├────────────────────────┼─────────────────────────────────────────────────────┤
│ LINODE_CLI_TOKEN       │ A Linode Personal Access Token for the CLI to make  │
│                        │ requests with. If specified, the configuration step │
│                        │ will be skipped.                                    │
│ LINODE_CLI_CA          │ The path to a custom Certificate Authority file to  │
│                        │ verify API requests against.                        │
│ LINODE_CLI_API_HOST    │ Overrides the target host for API requests. (e.g.   │
│                        │ 'api.linode.com')                                   │
│ LINODE_CLI_API_VERSION │ Overrides the target Linode API version for API     │
│                        │ requests. (e.g. 'v4beta')                           │
│ LINODE_CLI_API_SCHEME  │ Overrides the target scheme used for API requests.  │
│                        │ (e.g. 'https')                                      │
└────────────────────────┴─────────────────────────────────────────────────────┘

CLI user management commands:
┌─────────────┬──────────┬────────────┐
│ configure   │ set-user │ show-users │
│ remove-user │          │            │
└─────────────┴──────────┴────────────┘

CLI Plugin management commands:
┌─────────────────┬───────────────┐
│ register-plugin │ remove-plugin │
└─────────────────┴───────────────┘

Other CLI commands:
┌────────────┐
│ completion │
└────────────┘

Available commands:
┌────────────────────┬───────────────────┬─────────────────┐
│ account            │ databases         │ domains         │
│ events             │ firewalls         │ images          │
│ kernels            │ linodes           │ lke             │
│ longview           │ managed           │ networking      │
│ nodebalancers      │ object-storage    │ payment-methods │
│ phone              │ profile           │ regions         │
│ security-questions │ service-transfers │ sshkeys         │
│ stackscripts       │ tags              │ tickets         │
│ users              │ vlans             │ volumes         │
└────────────────────┴───────────────────┴─────────────────┘
Available plugins:
┌──────────────┬────────────────┬─────────────────┐
│ obj          │ ssh            │ firewall-editor │
│ image-upload │ get-kubeconfig │                 │
└──────────────┴────────────────┴─────────────────┘

To reconfigure, call `linode-cli configure`
For comprehensive documentation,visit https://www.linode.com/docs/api/
